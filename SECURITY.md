# 安全策略

## 报告漏洞

nexus-gateway 涉及 API 密钥和 provider 凭证管理。如发现安全漏洞，请按以下步骤报告：

1. **请勿公开披露** — 不要提交公开 Issue，不要发到讨论区
2. **发送邮件至项目维护者**（见 GitHub 仓库主页的联系方式）
3. 描述漏洞详情：影响范围、复现步骤、潜在风险

## 预期响应时间

- 24 小时内确认收到报告
- 7 天内发布修复或缓解方案
- 修复后公开致谢（如报告者同意）

## 安全最佳实践

### 部署时

- `gateway.yaml` 已加入 `.gitignore`，**永远不要提交**到版本控制
- API 密钥通过环境变量 `${VAR}` 引用，而非硬编码在 YAML 文件中
- 使用 `gateway.yaml.example` 模板创建配置，填入真实密钥

### 开发时

- 测试代码中的密钥使用 `sk-test`、`test-key` 等占位符（已全部替换）
- 不要硬编码本地绝对路径
- 提交前检查 `git diff` 是否遗漏了密钥或敏感路径

### 生产环境

- 网关鉴权通过 `Authorization: Bearer <gateway_key>` 实现
- 建议在生产环境使用 HTTPS 反向代理（如 Nginx）终止 TLS
- 定期轮换 provider API 密钥