[project]
name = "hermes-fiber"
version = "3.1.0"
description = "Hermes FiberRuntime — 运行时 Fiber 生命周期管理 + undo 栈 + 全局去重表"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = []

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"